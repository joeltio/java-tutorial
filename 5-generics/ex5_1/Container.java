package ex5_1;

import java.util.ArrayList;

public class Container<T> {
    // Task: Create a generic class, Container, which does the same thing
    // as ArrayList (add(item) and get(index) is enough).
}

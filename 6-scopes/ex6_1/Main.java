package ex6_1;

public class Main {
    public static void main(String[] args) {
        // THAT'S NOT WHAT I MEANT (exercise)
        // A scope of a variable is where the name of a variable is understood.
        // Programming languages are languages, and statements (sentences) are
        // understood within a certain scope (context).

        // Context and scopes are technically different in computer science, but
        // it doesn't really matter. For the sake of clarity, I will use them
        // accurately.

        // Get the pigs for me
        // > (at home) What pigs?
        // > (in a farm) Ok, sure

        // See the Tasks class
        Tasks tasks = new Tasks();
        tasks.main();
    }
}
